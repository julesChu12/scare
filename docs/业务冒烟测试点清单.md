# 业务冒烟测试点清单

- 来源脚本：`scripts/test_api.sh`
- 执行结果：`151/151` 通过
- 口径说明：这里的 `151` 是脚本本次实际执行并通过的测试点，包含 `HTTP 接口调用`、`接口响应字段断言`、`动态资源创建后的状态校验`，不是 `151 个唯一 URL`。
- 条件性未执行项：`POST /api/v1/b/notifications/:id/read` 本次因管理员通知列表为空被跳过，不在 `151` 个通过项内。

## 关键链路

- C端快速开通：发送验证码 -> 快速开通 -> 获取当前用户 -> Token 检查 -> Refresh -> 查看首单 -> 登出
- C端登录主链路：密码登录 -> 创建需求 -> B端查看 -> Staff 认领 -> Staff 完成 -> C端评价 -> 取消/编辑/状态流转
- Family 代提交链路：家属登录 -> 代老人提交需求 -> Staff 处理 -> Family 评价
- B端运营链路：用户/菜单/站点/围栏/新闻/Banner/老人档案/报表/统计 全量读写回归
- 任务转派链路：Staff1 认领 -> Admin 转派 -> Staff2 完成 -> SM 转派 -> Staff1 完成 -> 冲突分支校验

## 模块汇总

| 模块 | 通过项 |
| --- | ---: |
| 健康检查 | 2 |
| Admin 角色测试 | 40 |
| Admin 写操作测试 | 31 |
| Station Manager 角色测试 | 7 |
| Staff 角色测试 | 6 |
| C端公开 API 测试 | 8 |
| C端公开认证流程 | 7 |
| C端认证流程 | 8 |
| C端业务流程 | 13 |
| Family 角色测试 | 16 |
| 任务转派流程测试 | 13 |
| 合计 | 151 |

## 1. 健康检查（2）

覆盖接口族：`/health`、`/b/auth/me`（未认证边界）

- 服务健康检查
- 未认证 - B端 me 拒绝

## 2. Admin 角色测试（40）

覆盖接口族：`/b/auth/*`、`/b/users*`、`/b/menus*`、`/b/stations*`、`/b/zones*`、`/b/tasks*`、`/b/requests*`、`/b/banners*`、`/b/news*`、`/b/customers*`、`/b/permissions*`、`/b/roles*`、`/b/notifications*`、`/b/statistics/*`

- Admin 登录成功
- Admin - refresh token
- Admin - 获取当前用户
- Admin - 刷新Token
- Admin Token - 访问 C端 me 拒绝
- Admin - 用户列表
- Admin - 用户详情
- Admin - 用户更新(设置身份证)
- Admin - 用户筛选(role=staff)
- Admin - 用户筛选(station_id=1)
- Admin - 用户菜单
- Admin - 菜单树
- Admin - 站点列表
- Admin - 站点详情
- Admin - 围栏列表
- Admin - 任务池
- Admin - 任务池(所有站点)
- Admin - 任务池(指定站点)
- Admin - 我的任务
- Admin - 服务请求列表
- Admin - Banner列表
- Admin - 新闻列表
- Admin - 老人档案列表
- Admin - 权限树
- Admin - 角色权限
- Admin - 通知列表
- Admin - 任务列表
- Admin - 菜单详情(ID=:id)
- Admin - 新闻详情(ID=:id)
- Admin - 统计仪表盘
- Admin - 统计任务
- Admin - 统计请求
- Admin - 统计今日
- Admin - 用户详情返回 id_card_hash
- Admin - 用户详情返回 id_card_token
- Admin - 用户详情返回 id_card_masked
- Admin - 用户更新(身份证+hash互斥校验)
- Admin - 用户更新(错误id_card_token)
- Admin - 用户更新(id_card_hash保持原值)
- Admin - 用户更新(id_card_token保持原值)

## 3. Admin 写操作测试（31）

覆盖接口族：`/b/news*`、`/b/banners*`、`/b/stations*`、`/b/zones*`、`/b/users`、`/b/customers*`、`/b/requests*`、`/b/tasks/:id`、`/b/statistics/*`、`/b/reports*`

- Admin - 新闻创建 (ID=:id)
- Admin - 新闻详情(测试)
- Admin - 新闻更新
- Admin - 新闻删除
- Admin - 轮播图创建 (ID=:id)
- Admin - 轮播图更新
- Admin - 轮播图删除
- Admin - 站点创建 (ID=:id)
- Admin - 站点详情(测试)
- Admin - 站点更新
- Admin - 围栏创建 (ID=:id)
- Admin - 围栏更新
- Admin - 围栏删除
- Admin - 站点删除
- Admin - 创建用户(staff)
- Admin - 老人档案创建 (ID=:id)
- Admin - 老人档案详情
- Admin - 老人档案更新
- Admin - 老人服务记录
- Admin - 服务请求详情(ID=:id)
- Admin - 任务详情(动态ID)
- Admin - 统计概览
- Admin - 统计服务类型
- Admin - 统计趋势
- Admin - 统计效率
- Admin - 统计员工排名
- Admin - 报表预览
- Admin - 生成报表文件
- Admin - 历史报表列表
- Admin - 下载历史报表
- Admin - 删除历史报表

## 4. Station Manager 角色测试（7）

覆盖接口族：`/b/auth/me`、`/b/menus/user`、`/b/stations`、`/b/tasks/pool`、`/b/requests`、`/b/users`

- Station Manager 登录成功
- SM - 获取当前用户
- SM - 用户菜单
- SM - 站点列表
- SM - 任务池
- SM - 服务请求列表
- SM - 用户列表

## 5. Staff 角色测试（6）

覆盖接口族：`/b/auth/me`、`/b/menus/user`、`/b/tasks/pool`、`/b/tasks/my`、`/b/roles/admin/permissions`（拒绝分支）

- Staff 登录成功
- Staff - 获取当前用户
- Staff - 用户菜单
- Staff - 任务池
- Staff - 我的任务
- Staff - 角色权限(无权限)

## 6. C端公开 API 测试（8）

覆盖接口族：`/c/news*`、`/c/banners`、`/c/stations/:id`、`/c/stations/match`、`/c/geocode*`

- C端 - 新闻列表
- C端 - 新闻详情(ID=:id)
- C端 - Banner列表
- C端 - 站点公开详情(ID=:id)
- C端 - 站点匹配
- C端 - 站点匹配(POST)
- C端 - 逆地理编码
- C端 - 正地理编码

## 7. C端公开认证流程（7）

覆盖接口族：`/c/auth/send-code`、`/c/auth/quick-start`、`/c/auth/me`、`/c/auth/check`、`/c/auth/refresh`、`/c/auth/logout`、`/c/requests/:id`

- C端 - 发送验证码
- C端 - 快速开通成功
- QuickStart - 获取当前用户
- QuickStart - Token检查
- QuickStart - 刷新Token
- QuickStart - 查看服务请求
- QuickStart - 登出

## 8. C端认证流程（8）

覆盖接口族：`/c/auth/login`、`/c/auth/me`、`/c/auth/check`、`/c/auth/refresh`、`/b/auth/me`（端隔离拒绝）、`/c/profile`、`/c/notifications*`

- C端用户(张大爷)密码登录成功
- C端 - 获取当前用户
- C端 - Token检查
- C端 - 刷新Token
- C端 Token - 访问B端拒绝
- C端 - 更新个人资料
- C端 - 通知列表
- C端 - 通知标记已读

## 9. C端业务流程（13）

覆盖接口族：`/c/requests*`、`/b/requests*`、`/b/tasks*`、`/c/auth/logout`

- C端 - 创建服务请求 (ID=:id)
- C端 - 服务请求列表
- C端 - 服务请求详情
- Admin - 查看C端请求详情
- Staff - 认领任务
- Staff - 完成任务
- C端 - 评价服务
- C端 - 取消服务请求
- Admin - 管理员取消请求
- Admin - 更新请求信息
- Admin - 更新请求状态
- Admin - 查看状态更新后的请求
- C端(elderly) - 登出

## 10. Family 角色测试（16）

覆盖接口族：`/c/auth/login`、`/c/auth/me`、`/c/auth/check`、`/b/auth/me`（端隔离拒绝）、`/c/profile`、`/c/notifications`、`/c/news`、`/c/banners`、`/c/requests*`、`/b/tasks*`、`/c/auth/logout`

- Family(:family_user) 登录成功
- Family - 获取当前用户
- Family - Token检查
- Family Token - 访问B端拒绝
- Family - 更新个人资料
- Family - 通知列表
- Family - 新闻列表
- Family - Banner列表
- Family - 创建服务请求(代提交) (ID=:id)
- Family - 服务请求列表
- Family - 服务请求详情
- Family - 取消服务请求
- Staff - 认领Family请求任务
- Staff - 完成Family请求任务
- Family - 评价服务
- Family - 登出

## 11. 任务转派流程测试（13）

覆盖接口族：`/b/auth/login`、`/c/auth/login`、`/c/requests`、`/b/tasks*`

- Staff2(:staff2) 登录成功
- 转派测试 - 创建请求 (ID=:id)
- 转派 - Staff1认领任务
- 转派 - Staff2完成他人任务(应拒绝)
- 转派 - Admin转派给Staff2
- 转派 - 任务已转给Staff2(staff_id=:staff_id)
- 转派 - Staff2完成任务
- 转派 - 任务最终状态为completed
- 转派 - Staff2认领任务
- 转派 - SM(站长)转派给Staff1
- 转派 - Staff1完成站长转派任务
- 转派 - 转派给不存在用户(应失败)
- 转派 - 已完成任务转派(应失败)
